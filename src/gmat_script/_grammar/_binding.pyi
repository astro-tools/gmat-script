"""Type stub for the compiled ``_binding`` extension (built at wheel-build time)."""

from typing import Any

def language() -> Any:
    """Return the tree-sitter language capsule for the GMAT grammar."""
    ...
